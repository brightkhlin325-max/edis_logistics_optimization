# 收益預測 — 最佳解與演算法執行規劃

> 資料集：`data/raw/DataCoSupplyChainDataset.csv`（180,519 筆訂單、53 欄）
> 目標：預測每筆訂單的**收益（利潤）**
> 文件性質：演算法執行規劃（Plan），供實作與分工對照。

---

## 0. 一句話結論（最佳解）

> **用梯度提升樹（LightGBM / XGBoost）做迴歸，預測 `Order Profit Per Order`；
> 訓練前務必移除「利潤洩漏欄位」，並用「時間切分」做驗證。**

理由：這份資料是**大量、混合數值＋高基數類別、關係非線性**的表格資料 —— 這正是 GBDT（梯度提升樹）表現最佳的場景；線性迴歸只適合小型、強線性的資料（如 50 Startups），不適合當主力。

---

## 1. 問題定義

| 項目 | 內容 |
|---|---|
| 任務型態 | 監督式學習 — **迴歸（Regression）** |
| 目標欄位 | `Order Profit Per Order`（每筆訂單實際利潤，金額） |
| 樣本數 | 180,519 筆 |
| 評估指標 | **RMSE / MAE（金額誤差，主）** ＋ **R²（解釋力，輔）** |

### 為什麼選 `Order Profit Per Order` 當目標

| 欄位 | 意義 | 是否當目標 |
|---|---|---|
| `Order Profit Per Order` | 每筆訂單利潤（金額） | ✅ **採用** |
| `Benefit per order` | 與上面幾乎重複 | ⚠️ 重複欄位，視為洩漏 → 丟棄 |
| `Order Item Profit Ratio` | 利潤率(%) | ❌ 由利潤推導 → 洩漏 → 丟棄 |
| `Sales` / `Sales per customer` / `Order Item Total` | 營業額（收入，非利潤） | ❌ 非目標（但可當特徵） |

---

## 2. ⚠️ 最關鍵：資料洩漏（Data Leakage）

資料中存在一條會「抄答案」的關係：

```
利潤 (Order Profit Per Order) ≈ Sales × Order Item Profit Ratio
```

若把下列欄位留作特徵，模型 R² 會逼近 1.0，看似完美但實際無用（部署後拿不到這些欄位）。

**訓練前必丟（洩漏欄位）：**
- `Benefit per order`
- `Order Item Profit Ratio`
- 任何由利潤直接計算而來的欄位

**訓練前必丟（無資訊／個資／識別碼）：**
- 個資：`Customer Email`, `Customer Password`, `Customer Fname`, `Customer Lname`, `Customer Street`, `Customer Zipcode`
- 識別碼：`Customer Id`, `Order Id`, `Order Item Id`, `Order Customer Id`, `Category Id`, `Department Id`, `Product Card Id`, `Product Category Id`, `Order Item Cardprod Id`
- 雜訊／連結：`Product Image`, `Product Description`, `Order Zipcode`（多為空）

---

## 3. 特徵工程

### 3.1 數值特徵（直接使用）
`Sales`, `Sales per customer`, `Order Item Total`, `Order Item Discount`,
`Order Item Discount Rate`, `Order Item Product Price`, `Product Price`,
`Order Item Quantity`, `Days for shipping (real)`, `Days for shipment (scheduled)`,
`Late_delivery_risk`, `Latitude`, `Longitude`

### 3.2 類別特徵（編碼後使用）
| 欄位 | 基數 | 建議編碼 |
|---|---|---|
| `Type`, `Customer Segment`, `Shipping Mode`, `Delivery Status`, `Market`, `Order Status` | 低 | One-Hot 或 LightGBM 原生類別 |
| `Category Name`, `Department Name`, `Order Region` | 中 | LightGBM 原生類別 / Target Encoding |
| `Customer City`, `Order City`, `Order Country`, `Customer State`, `Order State` | 高 | Target Encoding（或直接交給 LightGBM `categorical_feature`） |

> **小技巧**：LightGBM 可原生吃類別欄位（`category` dtype），可省下大量 One-Hot，且通常更準。

### 3.3 日期特徵
從 `order date (DateOrders)` 拆出：`order_year`, `order_month`, `order_dayofweek`, `order_day`
（捕捉季節性與週期性需求 → 影響利潤）

---

## 4. 模型搭配（最佳解 vs 對照）

| 角色 | 模型 | 用途與原因 |
|---|---|---|
| 🥇 **主力（最佳解）** | **LightGBM Regressor** | 大型表格資料準度最高、訓練快、原生支援類別欄位、自動處理非線性與特徵交互 |
| 🥇 替代主力 | **XGBoost Regressor** | 與 LightGBM 同級，可二擇一或互相驗證 |
| 🥈 **對照基準** | **Random Forest Regressor** | 穩定、易調參，用來確認 boosting 是否真的勝出 |
| 📏 **下限基準** | **Linear Regression** | 最低標準線；若樹模型未明顯勝過它，代表特徵或流程有問題 |

> 不以線性迴歸為主力的原因：本資料非線性、含高基數類別與特徵交互，線性模型表達力不足。

---

## 5. 驗證策略

- **首選：時間切分（Time-based split）**
  以 `order date` 排序 → 前 80% 訓練、後 20% 測試。
  原因：實務上是用「過去」預測「未來」，時間切分最貼近真實情境、避免未來資訊洩漏。
- **輔助：K-Fold 交叉驗證（K=5）** 用於調參時的穩定度評估。
- **報告指標**：RMSE、MAE、R²（三者並列，金額誤差為主）。

---

## 6. 執行流程（Pipeline 演算法）

```
START
 │
 ├─ Step 1  載入 CSV（注意 encoding，建議 latin-1 / utf-8 容錯讀取）
 │
 ├─ Step 2  設定目標 y = Order Profit Per Order
 │
 ├─ Step 3  丟棄洩漏欄位 + 個資/ID/雜訊欄位（見 §2）
 │
 ├─ Step 4  特徵工程
 │            ├─ 日期拆解（year/month/dayofweek）
 │            ├─ 類別欄位轉 category / 編碼
 │            └─ 缺失值處理（數值補中位數、類別補 "Unknown"）
 │
 ├─ Step 5  時間切分 train / test（80/20）
 │
 ├─ Step 6  訓練三模型
 │            ├─ LinearRegression      （下限）
 │            ├─ RandomForestRegressor （對照）
 │            └─ LGBMRegressor         （主力）
 │
 ├─ Step 7  評估：對每個模型計算 RMSE / MAE / R²，輸出比較表
 │
 ├─ Step 8  解讀主力模型特徵重要度（feature importance）
 │            → 找出影響利潤最大的因素（如折扣率、運費天數、品類）
 │
 └─ Step 9  （選配）對 LightGBM 做超參數調整（Optuna / GridSearch）
END
```

---

## 7. 預期產出

1. **模型比較表**：三模型的 RMSE / MAE / R²。
2. **特徵重要度圖**：哪些因素最影響收益。
3. **最佳模型檔**：儲存 LightGBM 模型（`.pkl` / `.txt`）供後續預測。

### 預期表現（推論，非保證）
- 移除洩漏欄位後，LightGBM 的 R² 預期落在 **0.80 ~ 0.92** 區間（視特徵工程而定）。
- 若 R² 仍接近 1.0 → 代表還有洩漏欄位沒清乾淨,須回 §2 檢查。

---

## 8. 五人分工建議（對應現有專案）

| 角色 | 負責 |
|---|---|
| 資料工程 | Step 1–3：讀檔、清洗、洩漏欄位移除 |
| 特徵工程 | Step 4：日期、編碼、缺失值 |
| 建模 | Step 5–6：三模型訓練 |
| 評估 | Step 7–8：指標比較、特徵重要度 |
| 整合/報告 | Step 9 + 產出文件與圖表 |

---

## 9. 套件需求

```
pandas, numpy, scikit-learn, lightgbm, xgboost, matplotlib, optuna(選配)
```
